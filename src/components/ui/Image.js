import styled from 'react-emotion'

import * as ss from './styleSystem'

export const Image = styled('img')`
  ${ss.color}
  ${ss.space}
  ${ss.width}
  ${ss.height}
  ${ss.flex}
  ${ss.display}
  ${ss.justifyContent}
  ${ss.opacity}
  ${ss.position}
`
